<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/register_town_relative_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ecf0f1"
    android:clickable="true"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" >

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@color/actionbar"
            android:tag="actionBar" >

            <Button
                android:id="@+id/town_back"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_centerVertical="true"
                android:layout_toRightOf="@id/loginback"
                android:background="@drawable/text_down_selector"
                android:drawableLeft="@drawable/ic_action_up"
                android:gravity="center|center_horizontal"
                android:text="@string/select_town_back"
                android:textColor="#ffffff"
                android:textSize="22sp" />

            <TextView
                android:id="@+id/town_name"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_centerInParent="true"
                android:gravity="center"
                android:text="@string/select_town_back"
                android:textColor="#ffffff"
                android:textSize="22sp" />
        </RelativeLayout>

        <ListView
            android:id="@+id/townlist"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:cacheColorHint="#00000000"
            android:divider="#d1d5d8"
            android:dividerHeight="1dp"
            android:scrollbars="none" >
        </ListView>
    </LinearLayout>

</RelativeLayout>